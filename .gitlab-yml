image: docker:19.03-dind

services:
  - docker:19.03-dind

variables:
  DOCKER_TLS_CERTDIR: ""
  image_name: dnpis/azure-stack-cli

stages:
  - build

build:
  stage: build
  script:
    - docker build -t dnpis/azure-stack-cli:2.0.77 .
